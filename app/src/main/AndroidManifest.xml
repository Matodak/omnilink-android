<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.homeip.mleclerc.omnilinkanclient"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <uses-permission android:name="android.permission.INTERNET"/> 
    
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/APP_NAME"
        android:theme="@style/AppTheme" >
        <activity
            android:name="net.homeip.mleclerc.omnilinkanclient.OmniLinkClientMainActivity"
            android:label="@string/TITLE_ACTIVITY_MAIN" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="net.homeip.mleclerc.omnilinkanclient.model.omnilink.OmniLinkPreferenceActivity"></activity>
    </application>

</manifest>